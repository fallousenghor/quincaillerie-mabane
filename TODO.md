- [ ] Mettre à jour `src/utils/invoicePdf.js` pour coller visuellement “exactement” à la facture HTML fournie (dimensions, couleurs, épaisseurs, positions, tableau, footer).
- [ ] Vérifier que le rendu affiche bien 11 lignes par défaut.
- [ ] Supprimer l’import `jspdf-autotable` si inutilisé (optionnel selon style du projet).
- [ ] Lancer `npm run build` pour valider la compilation.
- [ ] Générer une facture dans l’app et vérifier le rendu PDF (impression/aperçu) vs exemple HTML.

